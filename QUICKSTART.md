# 🚀 Быстрый старт CrimeVision.kz

## Установка и запуск

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Загрузка тестовых данных

```bash
python load_sample_data.py
```

Этот скрипт создаст базу данных и загрузит тестовые данные из `data/sample_crimes.csv`.

### Шаг 3: Запуск сервера

```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

### Шаг 4: Открыть в браузере

Откройте: http://localhost:8000

---

## Использование

### Загрузка своих данных

1. Подготовьте CSV файл со следующими колонками:
   - `date` — дата (YYYY-MM-DD)
   - `region` — регион (например, "Алматы")
   - `city` — город (опционально)
   - `crime_type` — тип преступления
   - `latitude` — широта (опционально)
   - `longitude` — долгота (опционально)
   - `severity` — тяжесть 1-5 (опционально)

2. На главной странице нажмите "Загрузить данные"
3. Выберите CSV файл и загрузите

### Просмотр аналитики

- Используйте фильтры для выбора периода и региона
- Карта автоматически обновится с тепловыми зонами
- Графики покажут динамику и сравнение регионов
- Прогноз отобразится внизу страницы

---

## API Endpoints

### Получить статистику
```
GET /api/stats/summary?start_date=2024-01-01&end_date=2024-12-31&region=Алматы
```

### Получить список преступлений
```
GET /api/crimes?start_date=2024-01-01&end_date=2024-12-31&region=Алматы&limit=100
```

### Получить прогноз
```
GET /api/forecast?region=Алматы&months=3
```

### Оценка риска
```
GET /api/risk-assessment?region=Алматы
```

---

## Структура данных

### Регионы Казахстана

Поддерживаемые регионы:
- Алматы
- Астана
- Шымкент
- Алматинская область
- Актюбинская область
- Карагандинская область
- и другие...

### Типы преступлений

Примеры:
- Кража
- Грабёж
- Разбой
- Убийство
- Другое

---

## Устранение неполадок

### Порт 8000 занят

Измените порт в `main.py`:
```python
uvicorn.run("main:app", host="0.0.0.0", port=8080, reload=True)
```

### Ошибки с базой данных

Удалите файл `data/crime_vision.db` и перезапустите `load_sample_data.py`

### Нет данных на карте

1. Убедитесь, что данные загружены
2. Проверьте фильтры (возможно, период слишком узкий)
3. Убедитесь, что в данных есть координаты (latitude, longitude)

---

## Разработка

### Добавление новых функций

1. API endpoints — `app/api.py`
2. Бизнес-логика — `app/services/`
3. Интерфейс — `templates/index.html`

### Тестирование API

Используйте Swagger UI: http://localhost:8000/docs

---

**Удачи на хакатоне! 🎉**

